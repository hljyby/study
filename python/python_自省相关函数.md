```python
type(obj) # 获取对象类型
dir(obj) # 获取对象中所有属性（函数，类，变量）
help(obj) # 获取对象的帮助信息

isinstance(obj,类) # 判断obj是否为类的实例对象
issubclass(类，父类) # 判断类是否为父类的子类
hasattr(对象，属性或方法) # 判断对象中是否存在属性或方法
getattr(对象，属性或方法) # 获取对象中的属性或方法
setattr(对象，属性，属性值) # 设置对象的属性或方法

id(对象) # 获取对象在内存中的唯一标识符
```

